# Generated App Plans

Use this folder for safe, tenant-neutral Markdown application plans that are suitable to commit.

Do not store plans here when they contain API keys, tenant IDs, private URLs, customer names that should not be public, raw package payloads, screenshots, decoded `Resource` or `Sign` values, or other private data. For private/runtime plans, save the plan outside git and report the local path to the user.
